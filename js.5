let numeros = [4, 10, 2, 5, 7, 1];

let maior = numeros[0];

for ( let jp = 1 ; jp < numeros.length; jp++ ) {

    if (numeros[jp] > maior) {

        maior = numeros[jp];
    }
}

alert("O maior número é : " + maior);
