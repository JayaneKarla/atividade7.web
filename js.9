let resultado = 1;

for (let jp = 1 ; jp <= 5 ; jp++ ) {

    let num = Number(prompt("Digite o numero " + jp + ":"));

    resultado *= num;
}

alert("O resultado da multiplicacao dos 5 números é : " + resultado);
